build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: gunicorn brit.wsgi:application --bind 0.0.0.0:$PORT
  worker: celery worker --app=brit
release:
  image: web
  command:
    - bash release-commands.sh