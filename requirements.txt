# This file is kept for compatibility with tools that don't support pyproject.toml
# The authoritative dependency list is now in pyproject.toml
#
# To install production dependencies: uv pip install .
# To install with dev dependencies: uv pip install .[dev]
#
# If you need to generate a requirements.txt from pyproject.toml:
# uv pip compile pyproject.toml -o requirements.txt
# uv pip compile pyproject.toml --extra dev -o requirements-dev.txt
